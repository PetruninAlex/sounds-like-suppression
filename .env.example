# Template for local secrets. Copy to `.env` (gitignored) and fill in real
# values -- do NOT commit the filled-in .env.
#
#   cp .env.example .env
#
# Used by paper/run_all_steps.sh (sourced automatically) and by the VS Code
# launch configs via "envFile". Plain KEY=value lines, no `export`.

# Azure OpenAI (medical-term tagging in Step 5, sounds-like naming in Step 12).
AZURE_OPENAI_API_KEY_gpt5_5=...
AZURE_OPENAI_ENDPOINT_gpt5_5=...
deploymentid_gpt5_5=...
OPENAI_API_VERSION_gpt5_5=...
